## 7. מדריך הרצת סקריפטים ומבנה תיקיות

### מבנה תיקיות הפרויקט המדויק:

```text
DBProject_215043555_328276753
│   README.md                   # דוח הפרויקט הראשי ותוכן העניינים
│   01_Introduction.md          # מבוא
│   02_UI_UX.md                 # מסכים ו-UI/UX
│   03_Database_Design.md       # תרשימי מסד הנתונים
│   04_Data_Dictionary.md       # מילון נתונים
│   05_Data_Population.md       # אסטרטגיית אכלוס הנתונים
│   06_Backup_Restore.md        # מערך גיבוי ושחזור
│   07_Execution_Guide.md       # המדריך הנוכחי
│
└───שלב א
    │   backup_chess_19_05_2026.sql # קובץ הגיבוי הרשמי של מסד הנתונים
    │   createTables.sql        # סקריפט יצירת הטבלאות והאילוצים
    │   dropTables.sql          # סקריפט מחיקת הטבלאות
    │   DSD.png                 # תמונת תרשים DSD רלציוני
    │   ERD.png                 # תמונת תרשים ERD מושגי
    │   insertTables.sql        # קובץ אכלוס הנתונים המאוחד
    │   selectAll.sql           # פקודות שליפה ובדיקת תקינות נתונים
    │   אפיון (1).png עד אפיון (4).png # צילומי מסך אפיון UI מ-AI Studio
    │   גיבוי.png, שיחזור.png    # צילומי מסך של תהליכי הגיבוי והשחזור
    │   mockaroo_screenshot.png # צילום מסך הממחיש ייצור נתונים ב-Mockaroo
    │   python_screenshot.png   # צילום מסך הממחיש הרצת סקריפט פייתון לאכלוס
    │   manual_screenshot.png   # צילום מסך הממחיש הזנת נתונים ידנית
    │
    ├───Manual
    │       manual_inserts.sql  # סקריפט הכנסה ידנית של רשומות
    │
    ├───mockarooFiles
    │       mockaroo_data.sql   # נתונים שנוצרו בכלי Mockaroo
    │
    └───Programing
            generate_data.py    # סקריפט אכלוס נתונים ב-Python
```
        
### שלבי הרצה ובדיקת המערכת:
1. ודא כי שרת מסד הנתונים פעיל ומחובר באמצעות לקוח SQL כגון DBeaver.
2. פתח חלון סקריפט חדש והרץ תחילה את הקובץ `dropTables.sql` כדי לוודא סביבה נקייה מטבלאות קודמות (הקובץ כולל פקודות `CASCADE` על מנת למחוק קשרים בסדר בטוח).
3. פתח והרץ את הקובץ `createTables.sql` ליצירת מבנה הטבלאות הרלציוני, הגדרת מפתחות ראשיים, מפתחות זרים ואילוצי שלמות נתונים.
4. הרץ את סקריפט האכלוס `insertTables.sql` כדי למלא את המערכת בנתונים הנדרשים לבדיקה (הכולל את עשרות אלפי הרשומות).
5. להצגת הנתונים ובדיקת תקינותם, יש להריץ את פקודות השליפה מתוך הקובץ `selectAll.sql`.
