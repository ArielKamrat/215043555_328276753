## 2. איפיון מסכים (UI/UX)

איפיון ממשק המשתמש בוצע בעזרת Google AI Studio, במטרה להגדיר את תזרימי העבודה המרכזיים של השחקנים והמארגנים במערכת, ולוודא שכל שדה מידע שיוצג למשתמש הקצה אכן מיוצג במבנה בסיס הנתונים.

**קישור לפרויקט ב-Google AI Studio:** [צפייה באפיון המערכת](https://ai.studio/apps/8de7a5c0-0c30-425e-8c9e-dc47f1e832a7)

### פירוט המסכים המרכזיים שפותחו:

#### 1. מסך לובי ותחרויות (Tournament Discovery)
* **תיאור:** לוח בקרה מרכזי (Dashboard) המציג רשימה של תחרויות זמינות, עם אפשרויות סינון לפי בקרת זמן ופורמט.
* **רכיבי UI מרכזיים:** כרטיסיות טורנירים (Cards Grid), פסי סינון, כפתור הרשמה מהירה.
* **קישור לבסיס הנתונים:** שמות הטורנירים נשלפים מטבלת `Tournament`, ותאריכי ההרשמה נלקחים מהשדות `registration_open_date` ו-`start_date`.

#### 2. מסך ניהול סבבי תחרות (Tournament Live View)
* **תיאור:** מסך מעקב בזמן אמת עבור שחקנים וצופים, המציג את הסבב הנוכחי וטבלת הניקוד.
* **רכיבי UI מרכזיים:** טבלת שידוכים (Pairings Table) ורשימת מובילים (Leaderboard).
* **קישור לבסיס הנתונים:** מספר הסבב נשלף מטבלת `Round`, נתוני המשחקים המשובצים נלקחים מטבלת `Game` באמצעות החיבור ל-`round_id`.

#### 3. מסך לוח משחק פעיל (Active Game Interface)
* **תיאור:** ממשק המשחק המרכזי המציג לוח שחמט, שעוני זמן ורשימת מהלכים.
* **רכיבי UI מרכזיים:** לוח שחמט דינמי, שעוני עצר מותאמים.
* **קישור לבסיס הנתונים:** פרטי השחקנים נשלפים מטבלאות `Player` ו-`Game`. שעוני הזמן ובקרת המקור נלקחים מטבלת `TimeControl`.

#### 4. מסך היסטוריית משחקים וניתוח (Game Analysis Dashboard)
* **תיאור:** לוח בקרה המאפשר סקירה של משחקי עבר, ניתוח מגמות וסינון לפי תאריכים.
* **רכיבי UI מרכזיים:** מסנני תאריכים וטבלת היסטוריית משחקים.
* **קישור לבסיס הנתונים:** סינון המשחקים מבוצע על פי שדות התאריכים בטבלת המשחקים (`start_date`, `end_date`), ותוצאת המשחק נשלפת משדה `result`.

### צילומי מסך של ה-UI:
![Tournament Discovery Interface](שלב%20א/אפיון%20(1).png)
![Tournament Live View](שלב%20א/אפיון%20(2).png)
![Active Game Interface](שלב%20א/אפיון%20(3).png)
![Game Analysis Dashboard](שלב%20א/אפיון%20(4).png)
