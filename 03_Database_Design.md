## 3. עיצוב בסיס הנתונים (ERD & DSD) ונרמול

עיצוב בסיס הנתונים בוצע בשני שלבים מרכזיים באמצעות תוכנת ERDPlus, על מנת להבטיח תאימות מושגית ומבנית של הנתונים.

### א. תרשים ישויות-קשרים המושגי (ERD)
התרשים מתאר את הישויות במערכת (מלבנים), המאפיינים שלהן (אליפסות) והקשרים הלוגיים ביניהן (מעוינים). 
* **מאפייני תאריכים (DATE):** הפרויקט משלב שדות תאריך משמעותיים הפרוסים על פני הטבלאות. הוגדרו שדות מרכזיים כגון `Game.start_date`, `Game.end_date`, `Tournament.registration_open_date`, `Tournament.start_date`, ו-`Round.scheduled_date`.
* **ישויות חלשות וקשרי M:N:** המודל הרעיוני כולל קשרי רבים-לרבים מורכבים. הקשר בין שחקנים (`Player`) לתחרויות (`Tournament`) ממומש באמצעות ישות מקשרת בשם `Registration`. קשר זה מאפשר לעקוב אחר תהליך הרישום והסטטוס של השחקן בטורניר.

**תרשים ה-ERD:**
![Entity Relationship Diagram](שלב%20א/ERD.png)

### ב. תרשים הסכמה הרלציונית (DSD)
תרשים ה-DSD מתאר את מבנה הטבלאות הפיזיות בבסיס הנתונים, כולל הגדרת מפתחות ראשיים (PK) ומפתחות זרים (FK). החצים מצביעים על טבלאות האב הרלוונטיות.
*(הערה: התרשימים מייצגים את המודל המקורי, טרם ביצוע אופטימיזציה שכללה את צמצום טבלת המהלכים הבודדים לצורך ייעול שאילתות ושימוש בטבלת משחקים מורחבת).*

**תרשים ה-DSD:**
![Data Structure Diagram](שלב%20א/DSD.png)

### ג. החלטות עיצוב ונימוקי נרמול (3NF)
הסכמה המוצגת מנורמלת לתצורה השלישית (3NF), תוך הקפדה על העקרונות הבאים:
1. **נרמול 1NF (אטומיות השדות):** כל שדה מכיל נתונים יחידים ואטומיים בלבד.
2. **נרמול 2NF (תלות מלאה במפתח הראשי):** מניעת תלות חלקית במפתח ראשי מורכב. כל שדה תלוי לחלוטין במפתח הראשי של הטבלה לה הוא שייך.
3. **נרמול 3NF (מניעת תלות טרנזיטיבית):** מניעת שדות שתלויים בשדות אחרים שאינם מפתחות. כך למשל, פרטי הגדרות הזמן אינם נשמרים ישירות בטבלת `Game` אלא מנוהלים בטבלת ייחוס (Lookup Table) עצמאית בשם `TimeControl`, מה שמונע כפילות מידע ואנומליות תחזוקה בעת שינויים.
