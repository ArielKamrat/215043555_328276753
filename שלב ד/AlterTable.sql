-- ======================================================================================
-- AlterTable.sql
-- שלב ד: שינויי מבנה בטבלאות כדי לתמוך בלוגיקה התכנותית של הפונקציות והפרוצדורות
-- ======================================================================================

-- הוספת סטטוס לטורניר (פעיל או סגור)
ALTER TABLE Tournament 
ADD COLUMN IF NOT EXISTS status VARCHAR(20) DEFAULT 'Active';

-- הוספת עמודת ניקוד מצטבר להרשמה (מיועד להתעדכן על ידי פרוצדורת סגירת טורניר)
ALTER TABLE Registration 
ADD COLUMN IF NOT EXISTS total_score NUMERIC(5,1) DEFAULT 0.0;
